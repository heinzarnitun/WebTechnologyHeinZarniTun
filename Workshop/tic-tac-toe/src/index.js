import React from 'react';
import ReactDOM from 'react-dom/client'; // Capitalize 'ReactDOM'
import './styles.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById("root")); // Fix the typo here
root.render(<App />);
